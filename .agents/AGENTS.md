# Custom Project Rules

## Git Push Constraint
- **CRITICAL:** Do NOT run `git push` or attempt to push any changes to the remote git repository unless the user explicitly says **"lets push to git"**.
- This applies to all branches, including `main` and `krishna`.
